CREATE TABLE MATERIEL
(
  ID_MATERIEL NUMBER NOT NULL,
  NOM VARCHAR2(20),
  QUANTITE NUMBER,
  ID_LABOTP NUMBER NOT NULL
, CONSTRAINT MATERIEL_PK PRIMARY KEY
  (
    ID_MATERIEL
  )
  ENABLE
)
;

ALTER TABLE MATERIEL
ADD CONSTRAINT MATERIEL_LABOTP_FK1 FOREIGN KEY
(
  ID_LABOTP
)
REFERENCES LABOTP
(
ID_LABOTP
) ENABLE
;
